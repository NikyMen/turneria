// Aca fijo la metadata propia de la seccion de calendario.

export const metadata = {
  title: "Calendario | turnerIA"
};

export default function CalendarLayout({ children }) {
  return children;
}
